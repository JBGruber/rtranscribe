% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{transcribe_run}
\alias{transcribe_run}
\title{Transcribe audio with an existing session}
\usage{
transcribe_run(
  session,
  audio,
  task = "transcribe",
  language = NULL,
  target_language = NULL,
  timestamps = "auto",
  diarize = NULL,
  pnc = NULL,
  itn = NULL,
  keep_special_tags = FALSE,
  spec_k_drafts = NULL,
  family = NULL,
  interruptible = TRUE
)
}
\arguments{
\item{session}{A \code{transcribe_session} from \code{\link[=transcribe_session]{transcribe_session()}}.}

\item{audio}{A numeric vector of 16 kHz mono PCM samples in \verb{[-1, 1]}, or a
path to an audio file (decoded with \code{\link[=transcribe_read_audio]{transcribe_read_audio()}}, which needs
the \code{av} package).}

\item{task}{\code{"transcribe"} (default) or \code{"translate"}. Translation requires
a model whose capabilities declare support for it.}

\item{language}{Source-language hint as a short code such as \code{"en"} or
\code{"de"}. \code{NULL} (default) autodetects when the model supports it.}

\item{target_language}{Target language for \code{task = "translate"}.}

\item{timestamps}{Requested granularity: \code{"auto"} (default, the finest the
model supports), \code{"none"}, \code{"segment"}, \code{"word"} or \code{"token"}. Asking for
a granularity finer than the model can produce is an error; check
\code{\link[=transcribe_capabilities]{transcribe_capabilities()}}\verb{$max_timestamp_kind} first.}

\item{diarize}{Speaker attribution. \code{TRUE}/\code{FALSE}, or \code{NULL} (default) for
the model's own default. Only meaningful for models where
\code{transcribe_supports(model, "diarization")} is \code{TRUE}.}

\item{pnc, itn}{Punctuation/capitalisation and inverse text normalisation
toggles. \code{TRUE}/\code{FALSE}, or \code{NULL} (default) for the family default.}

\item{keep_special_tags}{Keep special vocabulary tags such as \verb{<|...|>} in
the returned text. Defaults to \code{FALSE}; the unprocessed decode is always
available as \code{result$raw_text}.}

\item{spec_k_drafts}{Speculative-decoding draft length. \code{NULL} (default)
uses the family's tuned value; \code{0} disables it.}

\item{family}{Family-specific options from \code{\link[=whisper_options]{whisper_options()}} and friends.}

\item{interruptible}{Whether \if{html}{\out{<kbd>}}Ctrl\if{html}{\out{</kbd>}}+\if{html}{\out{<kbd>}}C\if{html}{\out{</kbd>}} should cancel a
running transcription. Defaults to \code{TRUE}.}
}
\value{
A \link{transcribe_result} object.
}
\description{
The low-level run entry point. Use this when you want to reuse one loaded
model and session across many calls; see \code{\link[=transcribe]{transcribe()}} for the one-call
convenience wrapper.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
m <- transcribe_load_model("model.gguf")
s <- transcribe_session(m)
pcm <- transcribe_read_audio("speech.wav")
res <- transcribe_run(s, pcm, timestamps = "word")
res$segments
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=transcribe]{transcribe()}}, \code{\link[=transcribe_run_batch]{transcribe_run_batch()}}
}
