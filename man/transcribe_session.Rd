% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{transcribe_session}
\alias{transcribe_session}
\title{Create a transcription session}
\usage{
transcribe_session(model, n_threads = NULL, kv_type = "auto", n_ctx = NULL)
}
\arguments{
\item{model}{A \code{transcribe_model} from \code{\link[=transcribe_load_model]{transcribe_load_model()}}.}

\item{n_threads}{Number of CPU threads for ops that run on CPU. \code{NULL}
(default) lets the library pick.}

\item{kv_type}{Data type for the attention K/V cache: \code{"auto"} (default),
\code{"f32"} or \code{"f16"}.}

\item{n_ctx}{Optional cap on the decoder context window, in tokens. \code{NULL}
(default) uses the model's own maximum. This is a memory knob: lowering it
can lower the maximum audio length the session accepts.}
}
\value{
A \code{transcribe_session} object.
}
\description{
A session holds the decoding state for one stream of audio. Create one
session per concurrent transcription; a session may be reused for any number
of sequential runs, which avoids re-loading the model each time.
}
\details{
The session keeps a reference to \code{model}, so the model stays alive for as
long as the session does regardless of garbage-collection order.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
m <- transcribe_load_model("model.gguf")
s <- transcribe_session(m, n_threads = 4)
transcribe_session_limits(s)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=transcribe_run]{transcribe_run()}}, \code{\link[=transcribe_session_limits]{transcribe_session_limits()}}
}
