% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{transcribe_stream}
\alias{transcribe_stream}
\alias{transcribe_stream_begin}
\alias{transcribe_stream_feed}
\alias{transcribe_stream_finalize}
\alias{transcribe_stream_text}
\alias{transcribe_stream_state}
\alias{transcribe_stream_reset}
\alias{transcribe_stream_result}
\title{Incremental (streaming) transcription}
\usage{
transcribe_stream_begin(
  session,
  task = "transcribe",
  language = NULL,
  timestamps = "auto",
  diarize = NULL,
  pnc = NULL,
  itn = NULL,
  keep_special_tags = FALSE,
  family = NULL,
  commit_policy = "auto",
  stable_prefix_agreement_n = NULL
)

transcribe_stream_feed(stream, audio)

transcribe_stream_finalize(stream)

transcribe_stream_text(stream)

transcribe_stream_state(stream)

transcribe_stream_reset(stream)

transcribe_stream_result(stream)
}
\arguments{
\item{session}{A \code{transcribe_session}.}

\item{task}{\code{"transcribe"} (default) or \code{"translate"}. Translation requires
a model whose capabilities declare support for it.}

\item{language}{Source-language hint as a short code such as \code{"en"} or
\code{"de"}. \code{NULL} (default) autodetects when the model supports it.}

\item{timestamps}{Requested granularity: \code{"auto"} (default, the finest the
model supports), \code{"none"}, \code{"segment"}, \code{"word"} or \code{"token"}. Asking for
a granularity finer than the model can produce is an error; check
\code{\link[=transcribe_capabilities]{transcribe_capabilities()}}\verb{$max_timestamp_kind} first.}

\item{diarize}{Speaker attribution. \code{TRUE}/\code{FALSE}, or \code{NULL} (default) for
the model's own default. Only meaningful for models where
\code{transcribe_supports(model, "diarization")} is \code{TRUE}.}

\item{pnc, itn}{Punctuation/capitalisation and inverse text normalisation
toggles. \code{TRUE}/\code{FALSE}, or \code{NULL} (default) for the family default.}

\item{keep_special_tags}{Keep special vocabulary tags such as \verb{<|...|>} in
the returned text. Defaults to \code{FALSE}; the unprocessed decode is always
available as \code{result$raw_text}.}

\item{family}{Family-specific options from \code{\link[=whisper_options]{whisper_options()}} and friends.}

\item{commit_policy}{When the committed text is allowed to grow: \code{"auto"}
(default), \code{"on_finalize"} or \code{"stable_prefix"}.}

\item{stable_prefix_agreement_n}{How many consecutive hypotheses must agree
on a prefix before it is committed. \code{NULL} uses the library default (3).}

\item{stream}{A \code{transcribe_stream} from \code{transcribe_stream_begin()}.}

\item{audio}{A numeric vector of 16 kHz mono PCM samples.}
}
\value{
\code{transcribe_stream_begin()} returns a \code{transcribe_stream}.
\code{transcribe_stream_feed()} and \code{transcribe_stream_finalize()} return an
update list (invisibly for \code{feed}). \code{transcribe_stream_text()} returns a
list with \code{full}, \code{committed} and \code{tentative}.
}
\description{
Streaming is a mode on a session rather than a separate handle: begin a
stream, feed audio chunks as they arrive, then finalize. Requires a model
whose capabilities report \code{supports_streaming = TRUE}.
}
\details{
Two views of the text are available from \code{\link[=transcribe_stream_text]{transcribe_stream_text()}}:
\code{committed} is append-only and never rewritten, which is what a UI should
render; \code{full} is the model's current raw hypothesis and may be revised
anywhere.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
s <- transcribe_session(m)
st <- transcribe_stream_begin(s, family = moonshine_streaming_options())
for (chunk in chunks) transcribe_stream_feed(st, chunk)
transcribe_stream_finalize(st)
transcribe_stream_text(st)$committed
\dontshow{\}) # examplesIf}
}
