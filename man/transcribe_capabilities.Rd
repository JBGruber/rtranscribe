% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{transcribe_capabilities}
\alias{transcribe_capabilities}
\alias{transcribe_supports}
\title{Model capabilities and feature probes}
\usage{
transcribe_capabilities(model)

transcribe_supports(model, feature)
}
\arguments{
\item{model}{A \code{transcribe_model} from \code{\link[=transcribe_load_model]{transcribe_load_model()}}.}

\item{feature}{One of \code{"initial_prompt"}, \code{"temperature_fallback"},
\code{"long_form"}, \code{"cancellation"}, \code{"pnc"}, \code{"itn"} or \code{"diarization"}.}
}
\value{
\code{transcribe_capabilities()} returns a list. \code{transcribe_supports()}
returns a single logical.
}
\description{
\code{transcribe_capabilities()} reports the semantic properties a model declares:
supported languages, the finest timestamp granularity it can produce, whether
it can translate or stream, and its maximum input length.
}
\details{
\code{transcribe_supports()} probes a single named feature.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
m <- transcribe_load_model("model.gguf")
transcribe_capabilities(m)
transcribe_supports(m, "diarization")
\dontshow{\}) # examplesIf}
}
