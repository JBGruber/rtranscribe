% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{transcribe_result}
\alias{transcribe_result}
\title{Transcription results}
\description{
The object returned by \code{\link[=transcribe]{transcribe()}}, \code{\link[=transcribe_run]{transcribe_run()}} and each element
of \code{\link[=transcribe_run_batch]{transcribe_run_batch()}}. It is a list with the following elements:
}
\details{
\describe{
\item{\code{text}}{The clean transcript, as a single UTF-8 string.}
\item{\code{raw_text}}{The model's decode before family post-processing, with
any speaker markers, timestamp tokens and special tags still present.}
\item{\code{language}}{The language the model detected, or \code{""} when a
\code{language} hint was supplied or the model has no detection head.}
\item{\code{timestamp_kind}}{The granularity actually returned: \code{"none"},
\code{"segment"}, \code{"word"} or \code{"token"}. This is authoritative -- do not infer
granularity from row counts.}
\item{\code{segments}}{A tibble with \code{start}, \code{end} (seconds), \code{text} and
\code{speaker_id}.}
\item{\code{words}}{A tibble with \code{start}, \code{end}, \code{text} and \code{segment}.}
\item{\code{tokens}}{A tibble with \code{id}, \code{p} (confidence, may be \code{NA}),
\code{start}, \code{end}, \code{text}, \code{segment} and \code{word}.}
\item{\code{speakers}}{A tibble of "who spoke when" rows, populated when
diarization ran.}
\item{\code{timings}}{A list of stage timings in seconds.}
\item{\code{status},\code{status_message}}{The terminal status of the run. Non-zero
with a readable transcript means the result is partial.}
\item{\code{aborted},\code{truncated}}{Whether the run was cancelled, or stopped at
the model's generation cap before finishing.}
}
}
\seealso{
\code{\link[=transcribe]{transcribe()}}, \code{\link[=transcribe_run]{transcribe_run()}}
}
