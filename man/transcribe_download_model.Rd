% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{transcribe_download_model}
\alias{transcribe_download_model}
\title{Download a model}
\usage{
transcribe_download_model(
  name,
  dest = transcribe_cache_dir(),
  overwrite = FALSE,
  quiet = FALSE,
  ask = FALSE
)
}
\arguments{
\item{name}{Either a name from \code{\link[=transcribe_models]{transcribe_models()}}, or a direct URL to a
\code{.gguf} file.}

\item{dest}{Destination directory. Defaults to \code{\link[=transcribe_cache_dir]{transcribe_cache_dir()}}.}

\item{overwrite}{Re-download even if the file already exists.}

\item{quiet}{Suppress progress output.}

\item{ask}{If the model needs to be downloaded, ask the user to confirm
the download first.}
}
\value{
The path to the downloaded file, invisibly.
}
\description{
Downloads a GGUF model into the package cache and returns its path. A model
that is already present is not re-downloaded unless \code{overwrite = TRUE}.
}
\details{
A name that is not in the curated list is looked up in the full
\href{https://huggingface.co/handy-computer}{handy-computer} catalogue (once per
session, and only if \code{httr2} is installed) before it is rejected, so the
newer models listed by \code{transcribe_models(refresh = TRUE)} can be used
straight away.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
path <- transcribe_download_model("whisper-tiny")
m <- transcribe_load_model(path)
\dontshow{\}) # examplesIf}
}
