% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{whisper_options}
\alias{whisper_options}
\title{Whisper decoding options}
\usage{
whisper_options(
  initial_prompt = NULL,
  prompt_tokens = NULL,
  prompt_condition = NULL,
  condition_on_prev_tokens = NULL,
  max_prev_context_tokens = NULL,
  temperature = NULL,
  temperature_inc = NULL,
  compression_ratio_thold = NULL,
  logprob_thold = NULL,
  no_speech_thold = NULL,
  seed = NULL,
  max_initial_timestamp = NULL
)
}
\arguments{
\item{initial_prompt}{Free text used to bias decoding, for example a list of
proper nouns likely to appear in the audio.}

\item{prompt_tokens}{Integer token ids to use verbatim instead of
\code{initial_prompt}. Must not include the \verb{<|startofprev|>} marker.}

\item{prompt_condition}{How the initial prompt composes with
\code{condition_on_prev_tokens} on long-form runs: \code{"first_segment"} (default)
or \code{"all_segments"}. The latter requires \code{condition_on_prev_tokens = TRUE}.}

\item{condition_on_prev_tokens}{Carry the previous chunk's tokens into the
next chunk's prefix.}

\item{max_prev_context_tokens}{Cap for carried previous tokens (default 223).}

\item{temperature}{First-tier sampling temperature (default 0).}

\item{temperature_inc}{Temperature step for the fallback loop (default 0.2).}

\item{compression_ratio_thold}{Reject a decode whose gzip compression ratio
exceeds this (default 2.4).}

\item{logprob_thold}{Reject a decode whose average log-probability falls
below this (default -1).}

\item{no_speech_thold}{No-speech probability above which a chunk is treated
as silence (default 0.6).}

\item{seed}{Sampler seed for \code{temperature > 0}. \code{0} is nondeterministic.}

\item{max_initial_timestamp}{Cap on the first emitted timestamp, in seconds.}
}
\value{
A \code{transcribe_family_options} object.
}
\description{
Family-specific knobs for Whisper models, passed as the \code{family} argument of
\code{\link[=transcribe_run]{transcribe_run()}} or \code{\link[=transcribe]{transcribe()}}. Whisper ignores the generic \code{pnc} and
\code{itn} toggles.
}
\details{
Thresholds can be disabled individually by passing \code{Inf} (for
\code{compression_ratio_thold} and \code{no_speech_thold}) or \code{-Inf} (for
\code{logprob_thold}), which reproduces the "all thresholds off" behaviour of
HuggingFace's \code{generate()}.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
transcribe_run(s, pcm, family = whisper_options(
  initial_prompt = "Kubernetes, PostgreSQL, nginx",
  temperature = 0
))
\dontshow{\}) # examplesIf}
}
