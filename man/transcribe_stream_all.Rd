% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{transcribe_stream_all}
\alias{transcribe_stream_all}
\title{Stream a whole audio vector in chunks}
\usage{
transcribe_stream_all(
  session,
  audio,
  chunk_seconds = 1,
  family = NULL,
  language = NULL,
  progress = NULL,
  on_update = NULL,
  ...
)
}
\arguments{
\item{session}{A \code{transcribe_session}.}

\item{audio}{A numeric vector of 16 kHz mono PCM samples.}

\item{chunk_seconds}{Length of each fed chunk, in seconds.}

\item{family}{Family-specific options from \code{\link[=whisper_options]{whisper_options()}} and friends.}

\item{language}{Source-language hint as a short code such as \code{"en"} or
\code{"de"}. \code{NULL} (default) autodetects when the model supports it.}

\item{progress}{Whether to show a progress bar. Defaults to \code{TRUE} in
interactive sessions.}

\item{on_update}{Optional function called after each chunk with the current
committed text and the update list. Useful for live display.}

\item{...}{Further arguments passed to \code{transcribe_stream_begin()}, such as
\code{timestamps} or \code{commit_policy}.}
}
\value{
A \link{transcribe_result}.
}
\description{
Convenience wrapper that drives the full streaming lifecycle over one audio
vector, which is mostly useful for testing a streaming model and for
simulating live input.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
transcribe_stream_all(s, pcm, chunk_seconds = 1)
\dontshow{\}) # examplesIf}
}
