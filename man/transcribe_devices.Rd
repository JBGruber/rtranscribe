% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/devices.R
\name{transcribe_devices}
\alias{transcribe_devices}
\alias{transcribe_backend_available}
\title{Compute devices and backends}
\usage{
transcribe_devices()

transcribe_backend_available(kind)
}
\arguments{
\item{kind}{One of \code{"auto"}, \code{"cpu"}, \code{"cpu_accel"}, \code{"metal"}, \code{"vulkan"}
or \code{"cuda"}.}
}
\value{
\code{transcribe_devices()} returns a tibble with one row per device.
\code{transcribe_backend_available()} returns a single logical.
}
\description{
\code{transcribe_devices()} lists the compute devices registered with the runtime.
\code{transcribe_backend_available()} probes whether a named backend can be used.
}
\details{
The default source build of this package is CPU-only, so \code{transcribe_devices()}
normally reports a single CPU device. A GPU backend has to be compiled in at
install time — \code{TRANSCRIBE_R_VULKAN=1}, \code{TRANSCRIBE_R_CUDA=1} or
\code{TRANSCRIBE_R_METAL=1} with a source install (see the README) — after which
its devices appear here.

\code{transcribe_backend_available()} is a device probe rather than a build probe:
it is \code{TRUE} only when the backend was compiled in \emph{and} a usable device is
present, so a Vulkan build on a machine with no Vulkan driver still answers
\code{FALSE}.
}
\examples{
transcribe_devices()
transcribe_backend_available("cpu")

}
