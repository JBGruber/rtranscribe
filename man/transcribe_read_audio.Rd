% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audio.R
\name{transcribe_read_audio}
\alias{transcribe_read_audio}
\title{Read an audio file as 16 kHz mono PCM}
\usage{
transcribe_read_audio(path, sample_rate = 16000)
}
\arguments{
\item{path}{Path to an audio file.}

\item{sample_rate}{Target sample rate. transcribe.cpp only accepts 16000, so
changing this is almost always a mistake.}
}
\value{
A numeric vector of mono samples in \verb{[-1, 1]}.
}
\description{
Decodes and resamples an audio file into the numeric vector the transcription
functions expect. Any format \code{ffmpeg} understands works (wav, mp3, m4a, flac,
ogg, and the audio track of video files).
}
\details{
Requires the \code{av} package. Install it with \code{install.packages("av")}.
}
\examples{
\dontshow{if (requireNamespace("av", quietly = TRUE)) withAutoprint(\{ # examplesIf}
wav <- system.file("extdata", "jfk.wav", package = "rtranscribe")
pcm <- transcribe_read_audio(wav)
length(pcm) / 16000 # duration in seconds
\dontshow{\}) # examplesIf}
}
