% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{transcribe_accepts_options}
\alias{transcribe_accepts_options}
\title{Check whether a model accepts a set of family options}
\usage{
transcribe_accepts_options(model, options)
}
\arguments{
\item{model}{A \code{transcribe_model} or \code{transcribe_session}.}

\item{options}{A \code{transcribe_family_options} object.}
}
\value{
A single logical.
}
\description{
Family options are only valid for the model family they were written for,
and only in the slot (run or stream) they belong to. Probe before passing
them, so a mismatch becomes a clear message rather than an error from the
native layer.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
transcribe_accepts_options(m, whisper_options(initial_prompt = "hi"))
\dontshow{\}) # examplesIf}
}
